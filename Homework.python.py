first = "Alex"
last = "O."
age = 25

print first, last, age

print "------------------------"

sideA = 12.55
sideB = 17.85

sum = (sideA * sideA) + (sideB * sideB)

import math 

sideC = math.sqrt(sum)

print "side C = ", sideC

print "------------------------"

operand1 = 95
operand2 = 64.5

addition = operand1 + operand2
subtraction = operand1 - operand2
mult = operand1 * operand2
division = operand1 / operand2
mod = operand1 % operand2

print operand1, "+", operand2, "=", addition
print operand1, "-", operand2, "=", subtraction
print operand1, "*", operand2, "=", mult
print operand1, "/", operand2, "=", division
print operand1, "%", operand2, "=", mod, "(<-this is the remainder after division)"

print "------------------------"

#Just replace the value of the operand variables and the equation will still work.